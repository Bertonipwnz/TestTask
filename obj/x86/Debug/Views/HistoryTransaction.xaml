<Page
    x:Class="TestTaskUWP.Views.HistoryTransaction"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestTaskUWP.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    
    mc:Ignorable="d">


   <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!-- <ListView 
			ItemsSource="{x:Bind Transaction.ts, Mode=OneWay}"
			HorizontalAlignment="Left" Height="636" Margin="135,27,0,0" VerticalAlignment="Top" Width="1000">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TransactionVM">
                    <Grid>
                        <TextBlock Text="{x:Bind Type_Transaction, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView-->

        <!--<controls:DataGrid x:Name="dataGrid" Height="600" AutoGenerateColumns="False" ItemsSource="{x:Bind local:Customer}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn 
            Header="Дата" 
            Width="SizeToHeader"
            Binding="{Binding }" 
            FontSize="20" />
                <controls:DataGridTextColumn 
            Header="Сумма" 
            Width="SizeToHeader"
            Binding="{Binding Transaction.Amount_Transaction }" 
            FontSize="20" />
                <controls:DataGridTextColumn 
            Header="Тип" 
            Width="SizeToHeader"
            Binding="{Binding Transaction.Type_Transaction}" 
            FontSize="20" />
                <controls:DataGridTextColumn 
            Header="Категория" 
            Width="SizeToHeader"
            Binding="{Binding Transaction.Category_Transaction}" 
            FontSize="20" />
                <controls:DataGridTextColumn 
            Header="Комментарий" 
            Width="SizeToHeader"
            Binding="{Binding Transaction.Comment_Transaction }" 
            FontSize="20" />
            </controls:DataGrid.Columns>

        </controls:DataGrid>-->
            <ListView x:ConnectionId='2' Name="TransactionsBind" Grid.ColumnSpan="2" Grid.Row="5">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" >
                        <TextBlock Text="{Binding FirstName}" />
                        <TextBlock Text="{Binding LastName}" Margin="5,0,0,0" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>



